# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (keep .env.example tracked)
.env
.env.*
!.env.example

# local data (SQLite fallback db, file uploads)
docs/cankao/
/data
/uploads

# pdfjs 第三方产物(cmaps + standard_fonts),由 postinstall 从 pdfjs-dist 同步
/public/pdfjs/cmaps
/public/pdfjs/standard_fonts

# drizzle
# 迁移基线重建时的本地备份(安全网,确认无误后可删)
/drizzle/*.backup.*

# typescript
*.tsbuildinfo

# vitest
coverage/

# editor
.vscode/*
!.vscode/extensions.json
.idea

# claude code local settings(本地权限/配置,不入库)
.claude/settings.local.json

# trellis developer identity (gitignored by convention)
.trellis/.developer

# impeccable-live-ignore-start
.impeccable/hook.cache.json
.impeccable/hook.pending.json
.impeccable/config.local.json
.impeccable/live/server.json
.impeccable/live/sessions/
.impeccable/live/previews/
.impeccable/live/annotations/
.impeccable/live/cache/
.impeccable/live/manual-edit-apply-transaction.json
.impeccable/live/manual-edit-events.jsonl
.impeccable/live/manual-edit-evidence/
.impeccable/live/pending-manual-edits.json
.impeccable/live/deferred-svelte-component-accepts.json
.impeccable-live.json
.impeccable-live/
node_modules/.impeccable-live/
src/lib/impeccable/ImpeccableLiveRoot.svelte
src/lib/impeccable/__runtime.js
src/lib/impeccable/[0-9a-f]*/
# impeccable-live-ignore-end
