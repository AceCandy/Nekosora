# 技术方案

优先使用 Next 路由级错误边界和已有 UI/i18n 原语，新增最小聊天加载失败与重试界面。完整核对 getVisibleBranch/getConversationComposerState 的其他调用方；如需区别缺失状态，用最小明确返回/错误契约，不扩大修改其他 actions。核心读取与可选读取分开处理，控制流跳转不可吞掉；复用脱敏函数。范围为 chat 页面/必要布局、两读取函数及新增边界、文案和测试。样式遵循 DESIGN.md，不重设计聊天页。

不新增依赖；按子任务独立提交和回滚。任何数据、安全、兼容性或产品行为超出 PRD 时暂停并确认，不将本轮完成描述为全仓技术债清零。

实施选择：加载日志只记录固定操作名，不复用通用异常脱敏函数。SQL 异常可能包含任意私密正文，正则脱敏不足以保证清除；跨边界抛出新的固定错误，不携带原异常或 cause。错误页使用现有 Button 和品牌 token，不增加视觉系统。
