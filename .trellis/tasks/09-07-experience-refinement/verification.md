# 验证记录

## 2026-09-08 保存即生效（用户已确认）

- 日常 SystemSettings / OutputMode / RenderStyle 保存改为生产 revision 并发校验及单事务提交：业务表、revision、applied history 同成同败；无变化不推进 revision。删除因本轮失去调用方的三个草稿投影函数。
- 所有设置 Section 读取生产配置，旧草稿不参与日常保存；旧草稿以默认折叠的恢复入口保留。历史按点击加载并鉴权，撤销展开变更、二次确认后原子生效，保留字段冲突检查。
- 独立只读复核 settings_save_review 未发现可确认的严重/中等问题；主线程复核事务、全部调用方、缓存与双语文案。补充说明：日常表单保留本地输入，服务端 trim 后未强制重置所有输入控件。
- 全仓 lint/typecheck/test 通过：root 23、queue 29、core 1104、web 647、gateway 27、worker 4（此后又补 2 个 history Action 测试，设置目录最终 25/25）；Core 原有 12 条 lint warning。普通单测中隔离数据库套件仍按环境跳过。
- 使用本机一次性 pgvector 测试容器，通过现有 localhost-only 隔离库脚本运行真实 PostgreSQL 集成：5 套、38 项全部通过，含 7 项 settings-control 测试。覆盖三种资源立即生效、无操作/缺失旧字段、无效引用、最后一步写历史失败全量回滚、同 revision 并发、旧草稿保留及显式发布、历史不可变、直接撤销和失效令牌。未连接业务库执行写入。
- 浏览器只读验证 390px 无页面/历史弹窗横溢，历史按点击加载，撤销点击只展开一个明确目标的确认表单，Esc 关闭；没有确认实际撤销。拦截并中止设置 POST 后提交测试输入，错误提示可见、输入保留、保存按钮恢复；随后移除拦截并导航丢弃测试输入。
- 3500 用户开发服务仍返回旧语言包，新文案/新增 key 的浏览器验收未完成。已询问是否允许重启该 Web 服务；在明确许可前未停止或重启用户 Web / Worker。未运行生产构建、真机或真实上游验证。
- 一次性测试容器及内存数据库已停止并清理。没有生成截图、认证状态文件、依赖或迁移；真实旧草稿与业务配置未修改。代码未提交，按 trellis-finish-work 保留任务，不自动归档或提交日志。
- 本轮等待工具调用出现错误：原生代理不得传给 exec 的 wait/cell_id；后续若原生 wait_agent 不在实际可调用集合，停止尝试替代接口，明确报告并等待代理主动消息。

## 2026-09-08 反馈修正

- 全仓 lint/typecheck/test 通过：root 23、queue 29、core 1104、web 644、gateway 27、worker 4；原有 38 项跳过、12 条 lint warning。
- 新增 no-op 清理回归先红后绿：旧字段不存在时 `null -> null` 不再阻断正常设置暂存；持久化 parser 仍拒绝空变更，版本/属主校验未修改。数据库只读确认 task/title_model、task/compact_model、rag/mem0_llm_model 均不存在，仅有对应 `_id` 键，符合失败条件。
- 聊天恢复目录整数索引滑杆，单测覆盖目录缺档、固定推理不可关闭、键盘模型选择。320px 下滑杆 ArrowRight 更新实际档位，ArrowLeft 恢复；浮层位于 x=8..312，无页面横溢。常显后台入口移除，用户菜单入口保留。
- 服务商帮助默认收起，问号点击后显示接入说明及三个原链接，Esc 可关闭。
- 模型编辑预览使用原生顶层；320px 面板 x=8..312，scrollWidth=clientWidth=302，无内容横溢；1280px 与触发器右对齐，垂直间距 4px。模板搜索打开后焦点进入搜索框；Esc 只关浮层并返回触发器，父编辑 dialog 仍打开。补 Popover 定位后交付焦点的单测。
- 实施后单独复核本轮 diff，检查了 merge 全部调用方、原生 popover 定位/焦点时序、目录档位映射和已有导航入口；`git diff --check` 通过。
- 本轮未验证真实设置写入/发布与上游绘图。`test:core-pg` 被 localhost-only 安全校验拦截，未创建测试数据库、未绕过限制；未跑生产构建或真机。
- 当前 3500 开发服务返回旧翻译字典，新增 `providers.setupHelp` 仍显示为键；完整导航和 no-store fetch 也未刷新。磁盘中英文 key 校验通过，帮助交互本身已验证；未修改 i18n 加载实现，未重启用户服务。需服务正常重启后复核新增翻译。
- 用户尚未确认将“草稿再发布”改为“保存即生效”；本轮仅修保存错误，不改变生产生效时机、不发布已有草稿。
- 本轮独立浏览器已退出登录并关闭，临时截图清理；没有启动新服务、没有业务配置写入。代码未提交，按 trellis-finish-work 保留任务不归档。

### Bug Analysis：不存在字段清理与弹窗浮层

1. 根因类别：保存为 B/E（跨层契约及隐含假设），把编辑输入中的无操作误当持久化非法变更；预览为 E/D（坐标系假设和浏览器覆盖缺口），关闭 Portal 没有消除祖先动画 transform。
2. 旧判断不足：统一“刷新草稿”报错不能证明并发冲突；DOM/类型检查不能证明 fixed 使用视口坐标。以失败回归、只读数据库字段、真实几何测量区分原因。
3. 预防：P0 共享 merge no-op 测试与持久化拒绝测试已补；P0 顶层/可见后焦点测试及 Esc 浏览器核验已完成；PG 组合场景已补但受环境限制未执行。
4. 同类影响：标题/压缩/记忆/UA 空值清理共用 merge；模板选择与预览共用 Popover，修复放在共享边界。
5. 知识沉淀：backend/settings-control.md 与 frontend/component-guidelines.md 已更新。当前仓库不创建模板副本，不自动提交。

## 已完成

- 全仓 `pnpm typecheck`、`pnpm test` 通过。测试原有 38 项跳过；新增日期边界、键盘、固定档位、查询结构、表单失败回归通过。
- 全仓 `pnpm lint` 通过，Core 原有 12 条 warning 未扩大修改；最后一次 Web lint/typecheck 与相关测试复跑通过。
- `git diff --check` 通过。
- 独立只读复核 `/root/experience_ui_review`：未发现指定 UI 改动中的可确认正确性缺陷；主线程补做浏览器验证并复核后续小改动。
- 浏览器验证 320/390/768/1280 宽度：聊天未发生页面横溢，320px 模型面板完整位于视口；服务商 320px 表格宽 286px，名称、状态、主要操作直接可达。
- 模型搜索框 ArrowDown 进入第一项，继续 ArrowDown 到第二项，Escape 关闭并返回触发按钮。单测覆盖 Home/End/循环移动、非推理和固定推理。
- 服务商详情展开保留全部字段与操作；手机筛选字段完整可读且具名。
- 用浏览器拦截并中止保存请求模拟断网，未发送真实 Action：失败后弹窗与告警可见，名称、收起后的超时值保留；取消再确认可正常退出。
- 用量先显示筛选、摘要、明细，图表默认收起；自定义日期在错误标签链接中保留。日期与来源控件具名，主表滚动区可键盘聚焦。
- 图像无模型空态给出个人模型配置链接与联系管理员说明；英文 API 示例不含真实密钥。
- 新增中英文文案通过真实完整页面导航核验。开发热更新下，同源客户端导航可能保留旧根布局消息，完整页面导航后新消息正常；未为此改动国际化加载实现。

## 未验证与风险

- 未调用真实上游、未写入真实服务商/模型/密钥数据，真实新增到调用的端到端流程未执行。
- 路由失败恢复由代码复核覆盖；真实浏览器断网回归执行在服务商表单。
- 未执行生产构建、真机软键盘、安全区域、触控仿真或性能基准；不能将窄屏模拟等同于真机验收。
- 自动可访问性检查仍提示既有禁用分页文案的低对比度；未将此次工作扩展为全站可访问性清零。
- 最近模型仅保留当前页面选择，不跨刷新/账户持久化。

## 清理与交付

- 验证账号已退出，独立浏览器已关闭，本轮三张临时截图已删除。
- 尝试的独立验证服务未启动成功且已退出；用户原有 3500 服务未停止。
- 没有新增依赖、提交、发布或数据库迁移。按 trellis-finish-work 的未提交工作约束保留当前任务，不执行自动归档/日志提交。
