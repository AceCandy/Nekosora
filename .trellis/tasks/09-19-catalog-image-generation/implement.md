# 执行与验证

1. 扩展目录类型与共享出图选项，更新管理员保存动作和选择控件；测试权限、默认兼容、JSONB 合并与 pi 同步保留。
2. 在共享图像执行链添加 Responses SDK 适配；验证 URL、工具请求体、返回图片与用量、协议拒绝、无图片失败、参数边界、取消与现有 Images 回归。
3. 图像页透传目录格式并约束数量/尺寸；两条 HTTP 入口接受支持的尺寸；验证切换模型时不带入非法参数。
4. 执行 pnpm check / pnpm test，独立只读复核，记录真实数据库、浏览器与上游调用的验证范围。
