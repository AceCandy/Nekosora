# 技术方案

复用项目已有 Zod/消息契约，缺少时使用端点局部 schema，不引入通用验证框架。HTTP 输入错误返回 400；MCP JSON 解析失败保留 -32700，结构错误 -32600，已知方法参数错误 -32602。兼容现有合法可选字段与工具级错误。实施前补测试证明 null 解构/字段访问问题，再逐端点修复。数字字符串/小数 n 拟明确拒绝，须随最终规划获批。

每批单独检查、提交与回滚；无需数据迁移。不删除现有测试。
