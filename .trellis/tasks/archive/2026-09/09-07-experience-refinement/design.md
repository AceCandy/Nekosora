# 体验优化设计

保存即生效（已确认）：日常写入以生产 revision 校验并发，在同一事务中基于生产值生成变更、校验、写配置和不可变历史、推进 revision。旧草稿不参与日常读写，保留显式恢复入口。历史撤销复用字段级冲突检测，确认后直接提交；缓存失效仅在提交后执行，失败不能误报数据库回滚。历史由管理员点击入口后读取。

依据：`.impeccable/critique/2026-09-07T11-55-06Z__apps-web-src.md`，用户已确认继续实施。

- 接入：可见控制台入口、按任务分组的导航及页面内下一步链接；复用模型匹配/路由测试，不建立第二套配置状态机。原生 details 折叠高级参数，字段持续挂载。
- 用量：明细筛选优先，紧凑统计摘要与按需图表；明确统计仅按时间与用户范围，其他条件作用于明细，不伪装为全筛选统计。
- 窄屏：服务商主行保留名称、状态、详情/操作，低优先级字段移入展开区；输入框在手机始终两层。
- 模型：复用 Popover 和 coordinator 回调；最近选择仅为本地展示偏好，失效 ID 忽略；能力仅来自 ModelOption；使用原生离散选择控件表达目录档位。
- 风险边界：不修改鉴权、真实配置数据、模型能力定义、流式/滚动原语。所有旧操作继续可达。

实施补充：图表的 chartRange 原先同时控制相对日期和分桶，与所选昨天/自定义日期不一致。三个聚合函数新增可选日期边界，复用 buildUsageWhere；原有未传边界调用保持相对区间默认行为。切换明细/错误保留自定义日期及用户范围。

服务商与路由表单改为等待 Action 成功后关闭；失败保留输入并给出行内提示，pending 锁定字段与关闭操作。没有变更服务端写入契约。

反馈修正：移除 Sidebar 的常显后台链接，保留用户菜单的原入口；ModelConfigPicker 以原生 range 的整数索引映射目录档位；ProvidersManager 用已有 Popover 展示帮助。原生 dialog 内的 Popover 使用 manual popover 顶层，避免 modal-pop 动画创建的 fixed containing block，并在定位可见后交付搜索焦点。模板预览窄屏限宽，模型表单窄屏单列。settings-control 的 merge 接受空输入作为无操作，持久化 parser 仍拒绝空变更；发布、权限及并发语义不变。
