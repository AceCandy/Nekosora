# Chat 阅读连续性

用户已确认实施上轮建议中优先推荐的两项。

## 验收条件

- 选段追问将选中文字作为 Markdown 引用追加到现有草稿；多行选段逐行引用，末尾留出问题输入位置。
- 追问后输入框获得焦点、光标位于末尾，不自动发送消息。
- 同一次运行的过程详情默认收起；用户展开后，进入回答、完成、失败或中断时保持展开。用户仍可手动关闭，新运行重置为收起。
- 更新相关回归测试与过程详情规范，通过项目检查。

仅修改以上交互，不调整排队、模型配置、排版或网关行为。

## 验证结果

- 已完成两项交互与回归测试，独立复核通过。
- `pnpm check`、`pnpm test` 通过；全仓 1944 项测试通过，45 项按原配置跳过。未执行真实 PostgreSQL 集成测试。
- Chromium 隔离夹具加载真实输入框与过程详情组件，并提取当前追问处理函数：验证草稿保留、焦点、光标末尾、不发送、完成保持展开、新运行收起。
- 浏览器发现先聚焦再清除正文选区会将光标重置为 0；已调整顺序，补调用顺序回归测试并更新规范。
- 未验证完整登录页面、移动设备软键盘或 Safari。临时夹具与浏览器已关闭，临时文件已删除。
- 代码已提交于 `8ed0ef4`；2026-09-17 核对验收记录后，按用户要求归档。此次仅收尾记录，未新增代码改动或重跑测试，以上未验证项仍保留。
