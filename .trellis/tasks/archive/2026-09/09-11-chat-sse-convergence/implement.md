# 执行计划

- [x] 读取四动作、共享工具事件投影、SSEHandlers 及终态/附件/状态规范。
- [x] 在现有四动作参数化夹具增加公共回填与错误顺序回归，先跑旧实现。
- [x] 扩展共享消费函数，删除四份重复事件处理与成功终态回填。
- [x] 运行定向与全量检查，独立复核动作差异和多会话隔离。
- [x] 同步规范与父路线图，报告未验证项及剩余兼容层。

## 验证与复核

- 新增 8 项参数化回归，旧 Store 先跑 66 项通过；重构后 Store + parser 81 项通过。
- 最终 `pnpm check` 通过；`pnpm test` 为 1853 passed / 42 skipped，未增加跳过。
- 最终 Web Turbopack 生产构建通过；Core 包边界批次的 Gateway/Worker 构建已通过，其后无运行时代码改动。
- 实现后独立阅读完整 Store diff：请求体、preflight/附件消费/建会迁移、catch/finally 分支未改变；重生成先回填真实 ID，再在流结束后选版本；共享 onError 先通知已收到错误，再 flush/追加，catch 仍去重。
- 四动作成功/失败/中断、工具撤回、finish 元数据、生成版本持久化与失败隔离、标题轮询、离线和附件契约测试均保留。
- 未进行真实 PostgreSQL、上游服务、浏览器手工验证或容器启动；未启动调试服务。构建输出由现有忽略规则保留在本地。
- 本批与 Core 包边界批次均已实现并验证；Core 工作提交为 `c8cb4a8`，SSE 工作提交为 `6ad37b9`。未推送。
