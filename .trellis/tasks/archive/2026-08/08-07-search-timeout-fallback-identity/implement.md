# 修复搜索超时回退与模型展示 - Implementation

1. 扩展搜索执行选项和 Hosted 路由身份回调。
2. 在 `searchWeb` 中实现 10 秒后端预算、30 秒总预算和跳过逻辑。
3. 在逻辑工具闭包中维护本次回答的超时后端集合。
4. 补充服务层超时回退、跨调用跳过和 Hosted 身份测试。
5. 更新 Web Search 规范。
6. 运行定向测试、Core 类型检查、完整 Core 测试、lint 和 diff 校验。
7. 独立复核取消、并发和身份边界。
