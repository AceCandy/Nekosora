# 执行计划

- [x] 核对 Core/Web 引用、公开导出、5 个 Web 本地适配及三个构建入口。
- [x] 机械迁移引用并补实际需要的显式 exports；同步 mock。
- [x] 删除源码 alias 配置，更新直接失效规范。
- [x] 验证路径映射及 `pnpm check`、`pnpm test`、三个应用构建。
- [x] 独立审查并记录结果，然后继续聊天 SSE 批次。

基线提交 `9d136d4`，工作区干净。用户已授权连续完成既有路线图各批。

## 验证结果

269 个源码/测试文件、879 处字符串引用改为内部相对路径或包导出；补 66 个实际消费的显式子路径，删除 10 份配置中的 Core alias。
独立复核用 TypeScript 解析新旧路径，879 处均指向相同实际文件；逐文件 hash 与计划替换结果一致，唯一额外变化是补齐 `time-range.ts` 原本缺失的文件末尾换行。
`pnpm check` 通过；测试 1845 passed / 42 skipped；Web、Gateway、Worker 构建通过。尚未验证真实 PG 和浏览器。
保留 HTTP 回滚路由、Drizzle Schema 入口和包内兼容转发，其职责不因别名移除而消失。不主动删除兼容层；应用配置已不依赖 Core 内部目录。
实现与验证完成，尚未提交，继续下一批 SSE 收敛。

最终补充：同步三个入口注释和数据库指南中的旧导入示例；仅注释/文档变化。SSE 批次完成后再次全量检查通过，测试更新为 1853 passed / 42 skipped，Web 构建再次通过。
