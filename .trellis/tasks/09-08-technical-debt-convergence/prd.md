# 技术债收敛路线图

## Goal

保持现有功能、协议和数据，按质量门禁、数据库类型、包边界与聊天重复控制流分批降低维护风险。

## Requirements

- 各批独立规划、验证和回滚；父任务只记录路线与集成验收。
- 第一批为 `09-08-quality-gate-convergence`：纳入测试类型检查、清零警告、修正失真规范。用户已确认先实施这一批。
- 后续依次治理数据库真实类型、应用与共享包导出边界、聊天四条生成路径的重复 SSE 处理；各批开始前明确具体范围。
- 不替换框架，不清理业务数据，不因静态引用缺失直接删除运行时依赖或数据库兼容层。

## Acceptance Criteria

- [x] 第一批质量门禁与规范收敛完成（实现与验证完成，已提交 `a4b77a9`，未推送）。
- [x] 数据库访问类型与直接调用方收敛（已提交 `9e4ac67`；无关历史强转留待后续）。
- [x] 跨包源码别名与重复入口收敛（数据库导入已提交 `4c4e4c7`；Core 导入已提交 `c8cb4a8`；用途仍有效的兼容入口保留）。
- [x] 聊天公共流式处理逻辑收敛，四类操作语义保留（实现与验证完成）。
- [x] 完成跨批集成验证并记录剩余风险。

## 审计基线

2026-09-08：`pnpm check` 通过但有 12 条 lint 警告；`pnpm test` 1,837 项通过、42 项 PG 测试跳过。只读纳入测试后 Core 有 56 条类型诊断、Web 有 15 条。该基线不代表真实 PostgreSQL、生产构建或浏览器已验证。

## 2026-09-11 集成验收与剩余风险

- Core 879 处模块引用迁移后逐一解析到相同原文件；应用构建和测试配置不再维护 Core/DB 源码别名。
- 四个聊天动作共享 SSE 事件与终态消费，保留请求准备、附件/标题、失败回滚与版本选择差异。
- 最终 `pnpm check` 通过，测试 1853 passed / 42 skipped；Web、Gateway、Worker 生产构建均通过。
- 历史局部 `any` 未全仓清零；Drizzle Schema 工具入口、HTTP 回滚路由、包内转发层按现有用途保留。没有因静态无调用而删除兼容路径。
- 真实 PostgreSQL、上游服务、浏览器与容器运行尚未验收；构建及 mock 测试不替代这些验证。
- 已规划各批实现与集成检查完成，工作提交后归档；提交信息见会话记录。
